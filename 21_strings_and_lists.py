# SPLIT FUNCTION
abc = 'With three words'
abc.split()  # -> ['With', 'three', 'words']

# split() can take an optional delimiter argument
# By default, it splits at whitespace and treats any number of spaces as just one delimiter


